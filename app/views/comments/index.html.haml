%h1 Listing comments

%table
  %thead
    %tr
      %th Body
      %th
      %th
      %th

  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.body
        %td= link_to 'Destroy', item_comment_path(@item, comment), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br
= link_to 'Back', items_path
